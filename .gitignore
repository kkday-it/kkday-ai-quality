node_modules/
.pnpm-store/
dist/
*.tsbuildinfo
.env
.env.local
*.log
.DS_Store
.playwright-mcp/
findings/
.venv/
.venv-promptlab/
# Prompt Lab 隔離 venv（openai Responses API + pydantic；離線評測用，不入 Git）
backend/data/
# 根層 /data：原始進線資料匯出（CSV，含個資/大檔，禁止 commit）
/data/
__pycache__/
*.pyc

# AI 法官 intake 真實樣本（含客戶對話/訂單ID 等個資，禁止 commit）
backend/fixtures/intake/*.csv
# 例外：全合成小量樣本（零真實客戶資料），供開發者對照新格式，允許 commit
!backend/fixtures/intake/conversations_sample_30col.csv

# config 線上編輯的 working-copy 備份（git 本身即版本歷史）
config/taxonomy/.backups/

# DB 遷移前全庫備份（本地 pg_dump·數百 MB·含真實資料，禁止 commit）
backend/backups/

# CodeRAG 本地索引
.coderag/

# 本地實驗產出（A/B 評測結果含真實評論文字，禁止 commit）
/tmp/

# DB seed（含真實資料·57MB·經 SEED_URL 網盤/Release 分發，禁止 commit；保留空目錄供 docker 掛載）
docker/seed/*.sql.gz
backups/
docs/prompts/prompts/*.zip
