# features — 功能模組（feature-based）

每個 feature 自成一夾，內含 `pages/` `components/` `composables/` `utils/` `constants/` `routes/` `types/`
（各有 barrel `index.ts`；對外從夾根 import、內部相對 import）。跨 feature 共用元件在 `src/components/`、
共用 store 在 `src/stores/`、API 層在 `src/api/`、**共用圖表層在 `src/shared/charts/`**（ECharts builders +
圖表契約 + PDF 導出；feature → shared 單向依賴，禁 feature 互相 import）。

| feature     | 內容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `judge/`    | **AI 法官主模組**：規則配置（RuleManager 版本化編輯：source_mapping 整體配置〔上傳表頭校驗〕/ 初判 Prompt〔polarity + C-1~6 六域，7 支 Prompt-as-Source md〕+ 歷史對比）、資料上傳、歸因列表（一列一 review + 多歸因堆疊 + 伺服器分頁 + 初判歸因批次 + 初判模型篩選 + xlsx 導出（可選「輸出結果版本」＝指定模型的歷史快照，多模型對比）+ `AttributionHistoryDrawer` 評論級歸因歷史時間軸〔a-timeline：初判快照／備註／初判失敗三類事件 + 變更徽章 + 評論級備註〕）、Prompt 調試（`PromptDebugger` 調試台：載入**最新草稿**〔草稿／正式版雙軌，`index.json` 的 `active_release` 才是線上口徑〕＋任意 IM session SSE 串流裁決＋欄位校驗/單次費用＋「存為新草稿」只寫進草稿區、**不改線上口徑**（要上線得在版本面板把該草稿升為正式版）；`PromptDebugBatchDrawer` 跑批抽屜——**輸入二選一**〔上傳 CSV/XLSX，或選反饋來源 + 貼一串 ID 由後端從來源表撈對話；DB 模式下 ID/對話欄名由來源註冊表決定，不再讓人填，啟動後回報「要了幾筆、查無幾筆」〕、**Prompt 版本可選**〔頁面當前內容 或 任一已存檔草稿/正式版，共用 `PromptVersionSelect`〕、進度輪詢、停止/斷點續跑/重跑、產物 CSV/JSONL 下載，產物同步落 `data/prompt_debug_batch/`；跑批記錄表格**按群組合併**〔多模型跑批的時間/輸入/Prompt 版本三個共用欄以 `span-method` 縱向合併，每個模型各佔一列可分別看進度、續跑、重跑、下載〕＋**耗時欄**〔累計執行時間，非牆鐘；分段執行時附段數〕；**併發已全自動**〔輸入框移除；ceiling 依 model 查表、執行期 AIMD 依 429 升降〕；上傳卡片改自繪 `#upload-item`（Arco 預設 item 會渲染一顆對本元件無效的「開始上傳」play 圖示）；`PromptReviewPanel` 人工評判——結果卡逐欄標對錯，標錯就地填正解〔控件與值域由後端 `output_schema` 反推，見 `utils/reviewControl.util`，不手抄 enum；受控四層再由後端 `output_cascade` 做 **L1 → L2 → L3 級聯收窄 ＋ 條件式 L2 → L4**（L4 三分支：[93] 出修改標的、跳出出子型、其餘只剩 `n/a`）——下拉只列已選上層那一支底下的值（上層取「標錯已填的正解，否則 AI 原判」），上層一改就清掉失效的下層正解，跳出分支的 L1／L2／L3 三層各自只剩單一合法值「其他」〕＋整體修改建議 →「存為案例」入**瀏覽器本地案例庫**（`stores/promptReviewCases.store.ts`：Pinia + localStorage，上限 `MAX_CASES` 200 則；案例是個人調試用的暫存語料而非團隊共享資產，2026-08-04 起後端不再落庫、也無 CRUD 端點，改寫/回歸由請求整包帶上 `cases[]`），存完就地顯示「前往改寫這則 →」直接開流水線①並預勾該案例（刻意不自動彈抽屜——常見動線是連標數則再一起餵給 AI）；`PromptReviseDrawer` **四步單向流水線**（2026-07-31 由三個平行 tab 改造；`a-steps` 為唯一導覽，`v-show` 分塊保留掛載）〔**①選案例**：看/勾/刪，展開列逐欄呈現「AI 判的 → 正解」｜**②AI 改寫**（`PromptRevisePanel`）：勾選案例 → 旗艦模型（`prompt_revise` 功能區）產定點補丁 → 逐條勾選（anchor 對不上/撞多處者顯示但不可套）→ 套用 → `MdTextDiff` 對照，產出**候選 Prompt**；串流空窗走 `AsyncSection`（原為 `:percent=0.999` 假進度）｜**③回歸驗證**（`PromptRegressionPanel`）：拿案例庫重跑候選/現行/任一歷史版（共用 `PromptVersionSelect`），回「修好/改壞/還是不對/守住」四格計分＋逐案例明細；改壞紅字與零改壞綠字都帶**可點動作**（回②調補丁 / 改用跑批複驗）｜**④定案發布**（`PromptReleaseStep`）：存為新草稿 → 就地升為正式版，與當前正式版 `MdTextDiff` 對照〕。<br>⚠️ **狀態全由抽屜持有**（三個 composable 都在抽屜呼叫、props 下傳），面板只讀不寫——④需要「候選版＋回歸結果＋草稿名」三份狀態才能判閘門，走 emit 往上傳會變成雙份真相。<br>⚠️ **閘門與失效**（純函式在 `utils/pipelineGate.util.ts`，有單測）：允許回退、不允許跳過；**候選 Prompt 變動或案例勾選集合變動 → 回歸結果失效、④重新鎖上**——沒有這道判斷，使用者可以「套補丁 A → 回歸綠燈 → 改成補丁 B → 直接發布」，等於拿 A 的綠燈發布 B。回歸標的被覆寫成 baseline/歷史版時同樣判失效（驗的不是候選版）。失效時**不清快照**（清了就無從顯示「這份結果是針對別的候選版跑的」），改為置灰＋收起結論性提示。<br>「存為新草稿」**從②移到④**：它是定案動作、前提是③的結果，留在②會逼使用者「跑完回歸再倒回上一步存檔」（使用者回報的那條 Z 字形路徑）。升版邏輯與版本列表抽屜共用 `usePromptRelease`。抽屜開合與步驟同步 `?revise=<step>`（沿用 `SettingsDrawer` 慣例），還原時 clamp 到當前可達的最高步）、歸因概覽（縱覽 + 各來源專屬概覽·KPI+漏斗+L1-L2 下鑽+趨勢+PDF 報表導出+初判模型篩選〔當前初判維度·KPI 文案揭露口徑〕）。 |
| `settings/` | 設定抽屜：**LLM 設定分頁（`LlmSettingsPanel`）＝三個供應商 tab**（OpenAI/Gemini/ByteDance），每 tab 內兩層——① 連線（固定一條 base_url+token，全項目共用，改動需 `settings.llm-config.manage`）② **模型配置**（`LlmModelConfigList` 手風琴：增/改/刪 + 拖動排序（復用 `useListDragSort`）；**名稱由規格自動衍生、不可編輯**，故沒有「複製」；所有配置一視同仁可直接編輯，唯一限制是「清單不可清空」，刪到功能區預設起點只會在確認框告知後果、不阻擋；日常操作免特殊權限。每筆配置的動作列統一在右下角＝**刪除 / 測試連線 / 儲存**，其中「測試連線」（綠色，需 `settings.llm-config.manage`）測的是**當前草稿值**、未儲存也能先驗）。tab 標題帶 token 狀態點。**刻意不設集中綁定區塊**——每個功能區的下拉本身就是設定該區預設（選了即存），再放一份等於同一件事的第二個入口；「哪些功能區在用這筆」已標在每筆配置的手風琴標題上。/ QC DB 連線（每環境 sit/stage/production 固定一條，全項目共用）。<br>**模型旋鈕已收斂進具名配置庫**：各功能區（`prejudge` 初判分類／`prompt_debug` Prompt 調試台／`prompt_revise` AI 定點改寫，SSOT＝`config/global/llm_model.json` 的 `areas[]`）頁面上只留一個 `@/components/LlmConfigSelect` 下拉選配置（`useLlmAreaConfig(area)`），不再逐項調旋鈕，**也不再各擺一顆測試連線鈕**（已收斂到設定面板的配置編輯區）。⚠️「哪個功能區用哪一筆」＝ DB `setting_master` 的 `llm_area_configs`，與配置內容 `llm_model_configs` **同為團隊共用單一份**：下拉選了就即時落庫（無儲存按鈕，樂觀更新＋失敗回滾），一個人換配置同事下次進頁面也會是新的——2026-07-31 拍板的取捨，不是 bug。回落三級：DB 綁定 → 出廠 `areaDefaults` → 清單第一筆。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `overview/` | 總覽儀表板（config-驅動 DashboardView + chartRegistry；三業務目標 config/presale/postsale）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `usage/`    | 💰 AI 消耗 dashboard（`llm_usage_lst` per-call 紀錄聚合：成本/token/模型/階段趨勢）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `auth/`     | 登入 / 註冊。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

技術鐵律（見 `.claude/rules/frontend-vue.md`）：UI＝Arco Design Vue、圖表＝vue-echarts、狀態＝Pinia、
樣式 Tailwind utility-first、按鈕依語義區分主次。
