; Prompt Lab 測試 rootdir 錨點：使本目錄成為 rootdir，避免載入 backend/tests/ 上層
; conftest.py（其依賴 sqlalchemy + PostgreSQL，屬後端 DB 測試環境）。
; Prompt Lab 為獨立模組，於隔離 venv（.venv-promptlab）執行：
;   .venv-promptlab/bin/python -m pytest backend/tests/prompt_lab
[pytest]
addopts = -q
python_files = test_*.py
