# backend/.env 範本 —— 複製為 backend/.env 後填入實際值（.env 已 gitignore，勿 commit）。
# 啟動時由 app/api/main.py 的 load_dotenv() 載入。此處僅放「機密 / 環境相關」值；
# 非機密的共用預設在 repo 根 config/global/（qc_db.json 連線目錄、llm_model.json 模型與供應商目錄）。
# ⚠️ dev 走 ./start.sh 即可零配置啟動——本檔的每一項都是選填 fallback，不設也起得來。

# ── LLM（fallback；優先級低於 DB 設定面板的 provider token）──
# 使用者未在面板填 token 時的 OpenAI key fallback
OPENAI_API_KEY=
# 使用者未在面板選 model 時的 model fallback
AI_JUDGE_MODEL=gpt-5-mini

# ── at-rest 加密 ─────────────────────────────
# 設定面板機密（provider_tokens / qc_passwords）落庫前的加密 passphrase（任意字串）。
# 未設＝明文落庫（dev 相容）；設定後新寫入即加密，既有列跑 scripts/tools/encrypt_user_secrets.py 轉換。
# ⚠️ 換 key 前先用 --decrypt 轉回明文再換，否則舊密文無法解讀。prod 由 ./start.sh prod 自動生成。
AIQ_SECRET_KEY=
