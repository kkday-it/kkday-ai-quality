# frontend PDB（比照 sake prod overlay：至少 1 pod 存活，防節點維護時全滅）。僅 frontend——理由見 frontend-hpa.yaml。
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: aiq-frontend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: aiq-frontend
