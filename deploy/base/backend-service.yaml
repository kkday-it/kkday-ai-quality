# AI 法官後端 Service（草稿）
apiVersion: v1
kind: Service
metadata:
  name: aiq-backend
  labels:
    app: aiq-backend
spec:
  selector:
    app: aiq-backend
  ports:
    - name: http
      port: 8100
      targetPort: 8100
