# 全量分類（after_sales_root_cause）Prompt 版本記錄

> 版本檔 append-only：升版＝目錄內新增一個 `YYYY-MM-DD-HHMMSS.md`（全文快照）＋本檔補條目；舊版檔不改不刪。
> **無版本指針**——線上口徑固定為檔名時間戳最新的那份（`backend/app/judge/prompt_debug_versions.py` 解析），
> 調試台「存為新版本」與人手丟檔皆走同一條路；調試台與批量跑批一律讀最新版，不提供版本切換。

## 2026-07-28-140632 — [COMM]「連線品質不佳或與方案不符」納入漫遊/路由 IP 限制與規格三軸詢問（表側新邏輯落地）

來源＝PM 更新《根因標籤架構》表 [COMM]`連線品質不佳或與方案不符` 該列（Definition／Include／Lv3 受控值／Examples 四欄同批改），本版把表側新口徑落成線上唯一口徑。

> 版本檔軌跡（本次撞上調試台併發存檔，記於此以免日後看 `ls` 誤解）：`134438`＝調試台當日另存的 C06/C07 微調，本次改動以它為基線；`135450`＝本次 COMM 改動初落版；`140445`＝調試台再存一版，帶進另一支 C06/C07 改動，但其編輯器緩衝載入於 `135450` 之前，把 OOT 子型 2 的最後一句修正**覆蓋回舊寫法**（該句正是下方「攔下的過度矯正」的解法）；`140632`＝以 `140445` 為底補回那一句，只差 1 行。四支改動至此合流，`140632` 為完整版。

驗證用真案（2026/6-7 母體，USIMSA 日本 unlimited eSIM，2026-06-28 供應商線）：[USER]「I got the eSIM to work and reset my phone but I'm not getting any service」→「For some apps I don't have service but others I do」→「Specifically TikTok」→ [SUP]「The product you use is a roaming product, and apps with IP restrictions may be difficult to use.」正解＝`連線品質不佳或與方案不符`＋`漫遊路由IP限制致特定服務不可用`。

- 洞在哪：①**受控值缺口**——舊 Lv3 只有速度/斷網/天數三值，漫遊 IP 這型無值可填。這不只是 prompt 字面問題：`likely_cause` 的 JSON schema enum 由 `config/ai_judge/after_sales_root_cause.json` 各類 `likely_causes` 聯集生成（`prompt_debug.output_schema`），值不進 config 模型**根本吐不出來**，只補 prompt 無效。②C17/C18 分界寫的是「完全連不上 vs 連得上但慢」，沒處理「**能上網但只有特定服務不通**」這一格；用戶開頭那句「not getting any service」字面正中 C17，實測舊版 3 票 **2 票落 `已啟用但無法連線`＋`裝好無訊號`**（category 僅 1/3）。③表新增的 Examples 兩條（「無限流量都是吃到飽不降速嗎，內容裡沒看到相關說明」「提早安裝天數怎麼算」）在舊版分別落 **OOT 售前** 與 **[104] 商品規格/使用規則事前確認**——連線商品的規格詢問沒有歸屬，被鄰類分食
- 改法（三個入口寫進 C18 `calibration`，軸線＝**連得上，但拿到的與方案預期有落差**，不要求真的故障、也不要求用戶抱怨）：①速度/流量/天數實際表現與方案不符；②**速度/流量/天數三軸的規格詢問或揭露爭議**（含客服當場答完仍算）；③漫遊/路由 IP 限制致特定服務不可用。分界一律綁**可觀測範圍**而非用戶措辭：整體沒網→C17、只有特定 app 不通→C18，並明寫「用戶開頭的『沒有服務』不作數，以他自己下一句『有些 app 可以有些不行』為準」
- 落點（本版快照）：總綱速查 [COMM] 條目補「連不上要分整體或局部」；OOT 子型 2 的例外由一種擴為兩種（＋連線商品規格三軸→[COMM]）；判例庫 [COMM] 簇新增三條 verbatim（漫遊 IP 正例、整體沒網反面對照、天數/降速算法詢問）；C17 **首次帶 `calibration`**（限定「整體沒網」）；C18 六欄全改（definition/include/exclude/examples 依表逐字，另加 calibration 與改寫 likely_cause_guide）；C19 `calibration` 補反向邊界（本類只收**操作與時程**，算法問題轉 C18），兩邊對稱以免各自漂移
- SSOT 同步：`config/ai_judge/after_sales_root_cause.json` C18 同批加受控值 `漫遊路由IP限制致特定服務不可用`（enum 70→71）＋六欄同步＋`product_opportunity` 補漫遊 IP 揭露；C17／C19 首次帶 `calibration`（連同 C18 共三類，`config/README.md` 校準清單由五類更新為八類）；域判官 `prompts/debug/domains/theme_comm/v2.md` 階梯分界三行同步（其 `<taxonomy>` 由 config 渲染，六欄自動跟上）
- 受控值命名決策：表 Lv3 末位寫「其他」，本庫**一律落成 `unclear`**（25 類受控清單 25/25 皆以 unclear 收尾，`operational_rules.unclear_rule` 也明寫模型判不出必須顯式輸出 unclear；只為這一類開「其他」會讓該欄統計多一個同義桶）。「其他」僅存在於 `modify_target_options`／`oot_subtype_options` 兩份跨類清單
- 驗證（gpt-5.4-mini，reasoning_effort=high，調試台 `/prompt-debug/stream` 實跑）：目標案**新版 5/5 正解**（category＋cause 全中，含落版後走線上路徑 2 票）；舊版同題 3 票僅 1 票對 category、cause 0/3（值當時不存在）。表 Examples 另兩型各 2/2 落到 C18（`速度慢或限速`／`天數或流量算法與預期不符`），舊版分別為 OOT 售前、[104]
- 回歸（`exam_first100` 金標 COMM 全數 5 筆 ＋ eSIM 颱風退款 1 筆，單票）：**category 6/6、likely_cause 6/6 全守住**，含兩筆最像會被新入口吸走的 row43（「什麼時候寄憑證？想提前裝」）、row99（「可以使用到什麼時候」）——皆維持 `連線商品使用方式或設定諮詢`＋`開通時機與規則諮詢`。另跑 4 則真實母體邊界探針（完全上不了網／能否撥打電話／原生卡 ICCID 與簡訊辨識／降速規格），新版各 2/2 與應然一致
- 過程中攔下的一次過度矯正：初版改寫 OOT 子型 2 的分界句時只列「連線商品的速度/流量/天數怎麼算歸 [COMM]」，反面被讀成「其餘連線商品規格詢問可以跳出」——真案「日本原生卡 ICCID 不是 81 開頭／什麼卡才能收簡訊」當場由 `連線商品使用方式或設定諮詢` 掉成 **OOT 售前**（舊版是對的，等於我方引入的新 miss，且與判例庫「連線商品的使用方式詢問一律 [COMM]」自相矛盾）。改法＝該處補一條硬規則明寫連線商品的使用/設定/功能/規格詢問**一律留在 [COMM]**、並把「原生卡與漫遊卡怎麼分辨」寫成留在 C19 的例子，重測 2/2 回到 C19
- 表側待辦（PM）：①C17`連線商品已啟用但無法連線` 的 Exclude 目前只寫「連得上但速度慢/與方案不符」，未涵蓋「能上網但特定服務不可用」，建議補一句指向 C18，否則下次 taxonomy 重生會掉回舊口徑；②C19`連線商品使用方式或設定諮詢` 的 Include「開通時機與規則諮詢」與 C18 新增的「提早安裝天數怎麼算」字面高度重疊（本版靠校準層切成「操作與時程 vs 天數算法」），建議表上直接切開；③Lv3 末位「其他」與本庫 `unclear` 的對應關係如非本意請告知

## 2026-07-28-114317 — 兩枚 mention 旗標回歸《定案表》純提及口徑（砍金標擬合的例外清單）

誤判案（PM 提供，SHIBUYA SKY 觀景台，英文 chatbot session）：罐頭逐條列出本商品專屬例外取消條款（含「facility 確認頂樓關閉後辦理**取消與退款**，約 7–14 個工作日」「即使頂樓關閉，46 樓迴廊仍營業」）→ [USER]「Can I cancel for free if the SHIBUYA SKY rooftop is closed?」。上版 3/3 判 `money_mention_flag=false`＋`fulfillment_mention_flag=false`；正解＝**兩枚皆 true**（category/cause 與其餘欄位不動，仍為 `商品規格/使用規則事前確認`＋`商品頁資訊不足`，見上版 2026-07-27-225310 判例）。

- 洞在哪：這兩行規則仍是**金標擬合版的例外清單**（money：「罐頭字面提及退款但下文無關／假設性規則詢問→false」；fulfillment：「假設性詢問／憑證未到但當場獲回覆→false」）。2026-07-27 傍晚已拍板「四欄回歸《AI 判定其他欄位定義》定案版字面＝**明確提到即 true、不侷限 [USER]**，砍罐頭例外／假設性例外／當場獲答例外」，但只在離線作業線落地，本版本庫（調試台與跑批的唯一口徑）沒同步。實測還證明例外清單**自己不自洽**：同型的「罐頭提退款＋[USER] 只問規則」在上版全吐 `money=true`（`exam_first100` row57／row86／row94），唯獨本案命中「假設性規則詢問」字面被壓成 false——同型案給相反答案，這欄的統計就不能用
- 改法：兩行整體換成定案表字面（true＝對話文字明確提到／false＝全文未明確提到），刪掉所有例外清單；另各補一句釘死實測搖擺點——**假設句/條件句與英文同義詞同樣算「明確提到」**，且不因「事件尚未發生」「部分設施仍營業」「當場已獲回覆」「[USER] 本人沒說」降為 false。只換定案表字面實測不穩（本案 money 2/3、fulfillment 1/3），補這句後 8/8
- 落點（本版快照）：只動「欄位判定規則」的兩行，全檔其餘逐字不動（diff 2 行）。判例庫**刻意不加**本案旗標判例——旗標是全域欄位、判例庫是 category 路由用，寫進去會變成關鍵詞牽引（同 2026-07-27-225310 的 #37 教訓）
- SSOT 同步：本次無需外擴——旗標不在 `config/ai_judge/after_sales_root_cause.json` 受控值內；域判官 `prompts/debug/domains/*/v2.md` 只裁 category/likely_cause 不出旗標；schema／校驗器不變（bool 無 enum、`validate_result` 無旗標規則）；`prompt_debug.OUTPUT_FIELDS` 欄卡 hint 原本就寫「不侷限 [USER]」，與新口徑一致
- 驗證（gpt-5.4-mini，調試台 `/prompt-debug/stream` 實跑）：本案候選稿 5/5 ＋ 落版後走線上路徑（`system_prompt` 留空＝取最新版）3/3＝**8/8 兩旗標皆 true**，且 theme/category/likely_cause/summary/keywords/sentiment/urgency/multi_issue/nac 全程與上版一致；上版同題 3/3 皆 false。`pytest tests/test_prompt_debug.py` 14 passed
- 回歸（`exam_first100` 六筆單票，選點＝罐頭提退款型 ×3、憑證未到當場獲答 ×1、純使用諮詢 ×1、不可抗力特例 ×1）：**結構欄 6/6 與上版逐欄相同**（含既有 miss row57 的 C03↔加購，非本次引入）。旗標按新口徑翻動一處：row79（憑證未送達 → `fulfillment` false→true）；row18 的「退款」只出現在轉真人罐頭的**範例句**（「例如『我要取消訂單』或『我要申請退款』」）→ 仍 false，證明新口徑不是裸關鍵詞比對。row88（純問怎麼用、無金額/履約字樣）兩旗標維持 false，無過度觸發
- 認帳的代價（延續「規格合規＞金標擬合」的既有取捨）：100 筆金標對這兩欄用的其實是「[USER] 自己提／真有金流爭議」口徑（row57／row86／row94 罐頭提退款皆 gold=false），改純提及後靜態估算 money 約 11 筆、fulfillment 約 3 筆會與金標相左，舊分數（money 93%／fulfillment 90%）不再可比。本版本庫線上無金標評分，定案表即真相源
- 同源未同步（本次刻意不動，各自需帶回歸）：`sentiment` 判準軸仍寫「實際發生且未化解的負面事件」（定案表＝[USER] 情緒方向）、`multi_issue_flag` 仍把「可否取消＋會退款嗎」列為 true（定案表明列此組＝同一事項→false）。兩者在本案不影響結果（neutral／false）
- PM 待辦：《AI 判定其他欄位定義》定案版這兩欄若真要「任何角色明確提到即 true」，請確認能接受罐頭政策文字（幾乎每通 chatbot 進線都會列退款條款）把 `money_mention_flag` 大量拉成 true；若本意是「[USER] 為錢進線／真有金流爭議」（＝100 筆金標的標法），則要回頭改定案表字面，本版連同 v3.1 的四欄決策一起回退

## 2026-07-28-104331 — 取消訴求「事由被證實為誤解」不再吸進 [101] 僵化，改歸 [104] 商品規格

誤判案（PM 提供，大阪通天閣門票／關西周遊券）：[USER]「沒選擇到日期不小心直接按到結帳付款，請幫忙協助取消訂單」→ BOT 照政策判死（訂購完成後不可取消、更改、不得退款）→ 轉真人 → [KKDAY]「此訂單商品**無須在訂購時選擇體驗日期**，憑證兌換期限自開票日算起 270 日內有效」→ [USER]「所以是不需要預約，只要在 270 天內使用完畢就可以了…那沒問題了 謝謝」。判成 `取消政策本身僵化`＋`規則就是不可退用戶不滿`＋`multi_issue_flag=true`；正解＝`商品規格/使用規則事前確認`＋`方案規格描述不清`＋`multi_issue_flag=false`。

- 洞在哪：C06 上版（2026-07-27-200500）的決勝軸校準寫成「對話已明確不可退＋用戶提取消 → **一律**本類，不論用戶事前是否知情、是否表達不滿、是否再爭取」。這條當初是為了止住 C06 被抽空（金標 100 筆 C06 掛零），代價是變成純字面觸發：只要 BOT 背了一次不可退政策就鎖定 C06，**沒有處理「取消訴求建立在錯誤前提上、經規則說明後訴求自行消解」這一型**。本案用戶其實沒下錯單（該商品本來就不用選日期），根因是開放日期券的規格沒讓他看懂，全程未爭政策合理性、最終無取消可辦——貼 C06 會把下游 action 從「商品頁資訊補強」誤導成「取消政策合理性檢視、手續費級距透明」（見表《各類診對後對應action》），且掩蓋第二個洞：BOT 只背取消政策，沒認出「本商品無須選日期、你沒下錯單」
- 改法：在 C06 的「一律本類」前加**唯一前置例外**，判準綁三個可觀測條件（不猜心理狀態，延續可退性軸的設計）：①用戶的取消事由是「自己弄錯了」的操作失誤前提；②客服/BOT **當場明確否定該前提**（「本商品無須在訂購時選擇體驗日期」「你買的是開放日期券」）；③結案落在使用/兌換規則說明且用戶不再要求辦理。三條全中 → [104] C03；**缺②**（只重申不可退政策、從未指出用戶認知有誤）或**誤解解除後用戶仍要辦取消/爭合理性** → 仍是 C06。同一條分界寫進 C03 當「第四種入口」，兩邊對稱以免各自漂移
- 落點（本版快照）：總綱速查新增 C03 第二條目（誤解型入口）＋[101] 取消卡點條目補「⚠️前置檢查」；根因匹配鐵律補「取消只是訴求動詞」延伸句；判例庫在「買錯了可以退嗎→判死」判例後緊接本案 verbatim ＋反面對照（分界＝**取消的事由有沒有被否定**）；C03 `calibration` 增第四種入口、C06 `calibration` 增前置例外＋收窄條件；`operational_rules.cancel_withdrawal_and_status` 由兩分界改三分界（撤回→OOT／流程中撤不回→退款進度／**事由被證實為誤解→C03**）
- 連帶修正兩個欄位漂移：①`multi_issue_flag` 實測 3/3 誤判 true（把「先要求取消」與「後確認使用規則」讀成兩個訴求）→ false 清單補此原型「同一張訂單的取消要求＋經說明後的使用規則確認＝同一個疑慮的前後兩段」；②`likely_cause` 硬規則 6 原把 `方案規格描述不清` 鎖死在「X 日是不是到貨時間」單一字面 → 改為**日期語意的單一誤解型**（含「沒選到日期就結帳」把開放日期券誤當須選日期券），連環詢問仍固定 `商品頁資訊不足`（該條防漂保留）
- SSOT 同步：`config/ai_judge/after_sales_root_cause.json` C03/C06 `calibration` 同步（第四種入口／前置例外，含 likely_cause 選值分界）；域判官 `theme_101/v2.md` 必棄權清單補本型（含反向別棄權過頭）、`theme_104/v2.md`「因取消而退款→非本域」補反向例外
- 驗證（gpt-5.4-mini，調試台 `/prompt-debug/stream` 實跑）：本案**新版 6/6 正解**（category＋cause＋`multi_issue_flag=false`＋`sentiment=neutral`，含首發煙囪測 1 票）；**舊版同題 3 票不穩**——2 票誤判 C06＋`規則就是不可退用戶不滿`＋`sentiment=negative`、1 票已歸 C03 但 cause 取 `商品頁資訊不足`，即舊版 category 1/3、完全命中 0/3
- 回歸（`exam_first100` 風險子集 n=29＝gold C03 全 4 筆 ＋ [101] 全家族 25 筆，單票）：**新版 26/29、舊版 27/29**。唯一差異 row3（颱風「若遇颱風可取消嗎→供應商因停船主動代取消」）經 4 票複驗確認屬**既有不穩區**：新版 3/4 命中 gold、舊版 1/4，單票剛好抽到反例，非本次引入。兩版共同 miss＝row21（不可改重訂 vs 退款進度）、row57（C03 vs 加購），皆為既有 miss。**gold C03 4 筆兩版皆守住，無過度矯正**（新增的誤解型入口未把任何 [101] 家族金標吸走）
- 排錯備忘：首輪回歸新版跑出 9 筆 `無 result 幀`，root cause 是**跑批當下同時在編輯 `config/ai_judge/after_sales_root_cause.json`**——後端每請求重讀該檔（`prompt_debug.load_taxonomy()` 無快取），讀到半寫入狀態即 `JSONDecodeError: Unterminated string`。往後跑批期間勿改 config SSOT
- 表側待辦（PM）：《根因標籤架構》C06 的 Definition「用戶知道規則但認為不合理而進線爭取」字面仍與本版校準（不看是否爭取）相左，且未涵蓋「事由被證實為誤解」的排除；建議 C06 Exclude 欄補一句「取消事由被客服證實為誤解（訂單其實無誤）→ [104] 商品規格/使用規則事前確認」，否則下次 taxonomy 重生會掉回舊口徑

## 2026-07-27-225310 — [104] 「商品規格/使用規則事前確認」判準由「客服答不答得出」改為「用戶問的標的」

誤判案（PM 提供，SHIBUYA SKY 觀景台）：BOT 罐頭逐條列出**本商品專屬**例外取消條款（「僅適用本商品且限透過 KKday 平台購買」「當日 23:59 前提交例外取消規定表單」「46 樓迴廊仍營業」「退款 7～14 個工作日」）→ [USER]「若 SHIBUYA SKY 頂樓關閉，可以免費取消嗎？」。判成 `OOT 跳出`＋`售前_商品資訊詢問`；正解＝`商品規格/使用規則事前確認`＋`商品頁資訊不足`。

- 三個洞：①C03 的 `calibration` 寫「資訊當場被順利解答→跳出（售前）」，把**客服答不答得出**當判準——這條與金標直接衝突：金標 #69（JR PASS 到貨日/兌換/期限連環問）、#94（7/20 才用需否取消＋兌換期限）都是 **BOT 一次照商品規則答完**，gold 仍是 C03；②OOT 子型 2 把「規則假設性詢問（若颱風怎麼退、風險書）」寫成泛稱，任何規則詢問一律被吸走；③子型 2 的「已購未購皆算」與《根因標籤架構》使用說明 §11(2) 售前/售後判準（已購商品的資訊詢問＝售後）字面相牴觸，且違反表硬規則「只要屬五大主題任一即不可跳出」。另：C03 在總綱「常見根因情境速查」中**完全沒有條目**，模型全程無正向牽引
- 改法：判準換成**用戶問的標的**（可觀測，不必猜客服能力）。本訂單商品「使用/兌換/取消」**規則條文怎麼適用**（能不能／要不要／期限到何時）→ C03；轉出只留三種：泛用天候/不可抗力退改政策詢問、非規則條文的商品資訊（配備「有沒有附」什麼／在哪領／會不會開／價格）、確認後要改→[93]。**商品專屬 vs 泛用**用可觀測字樣分界：對話出現「僅適用本商品」「限本平台購買」「專屬表單/專屬時限」＝商品專屬條款
- 落點（本版快照）：總綱速查新增 C03 條目（原本缺漏）；C03 `calibration` 重寫；OOT 子型 2 收窄「規則假設性詢問」為**泛用天候/不可抗力**並加唯一例外指回 C03；子型速記補「先過一關」；判例庫在「風險書」判例後緊接本案 verbatim 反面對照（分界＝條款是否商品專屬）
- SSOT 同步：`config/ai_judge/after_sales_root_cause.json` C03 首次帶 `calibration`（繼 C06/C07、C12/C13 後第三組）；域判官 `prompts/debug/domains/theme_104/v2.md` 邊界補「別反向棄權過頭」＋反向分界清單
- 驗證（gpt-5.4-mini，調試台 `/prompt-debug/stream` 實跑）：本案**新版 6/6 正解**（category＋cause＋`money_mention_flag` 一併由 true 修正為 false，符合本表「假設性規則詢問→false」），舊版 4/4 全誤判 OOT 售前。金標回歸（`exam_first100` 中 gold=C03 的 4 筆 ＋ gold=OOT/售前 的 14 筆，共 18 筆）：**單票全跑 舊版 14/19 → 新版 16/19**（含本案），且**無新增 miss**——舊版三個 miss（#12 補件、#57 多訴求、#70 機車配備）中 #70 反而修好
- 過程中攔下的一次過度矯正：初版把「商品**規格**」寫進判準，#70「機車有沒有附安全帽/手機支架」（gold＝OOT 售前，因表 Include 字面就有「方案設施範圍」）被吸進 C03（3 票 2 誤）→ 收窄為只講「規則條文」＋把安全帽反例寫成 verbatim，重測 #70 5/6（舊版基線僅 1/3）。另試過在子型 2 補「憑證領取地點與領取方式」，反而讓 #37（WOWPASS 領取）漂向 [104] 憑證類（2/3）→ 該句已移除（[104] C01 calibration 原就涵蓋），移除後 #37 回 3/3
- 表側待辦（PM）：《根因標籤架構》C03 的 Include「方案設施範圍」與金標 #70（配備詢問歸 OOT 售前）互斥，建議表上明確切開「配備清單有什麼＝售前」vs「使用規則條文＝C03」，否則下次 taxonomy 重生會掉回舊口徑

## 2026-07-27-200500 — [101] 「僵化 vs 揭露不清」決勝軸改為商品客觀可退性

誤判案（調試台實跑，英文 chatbot session）：「I didn't have time to go can I get a refund」→ BOT 依政策判死（憑證已開立不可取消/修改/退款、本政策無退款申請通道）。判成 `取消政策揭露不清`＋`下單前不知取消規則`；正解＝`取消政策本身僵化`＋`規則就是不可退用戶不滿`。

- 兩個洞：①舊口徑把 6 vs 7 綁在「用戶知不知道規則」——這是心理狀態，真實對話幾乎不留訊號，模型只能由訴求動詞反推「事前不知情」，結果 C06 被系統性抽空、C07 灌水（金標 100 筆中 C06 掛零可佐證）；②總綱鐵律直接寫「問『會不會退款』→ 揭露不清」，無可退性前提，本案一擊命中。線上表側自身也矛盾：C07 的 Definition 是「可退或有條件退」，Examples 卻放「沒注意到不可退條款」（＝不可退）
- 改法：決勝軸換成**商品客觀可退性**（BOT 回覆幾乎都會講明，可觀測、可複現，不必猜心理）。對話已明確不可退/高額手續費＋用戶提取消退款訴求 → C06（不論事前是否知情、是否表達不滿、是否再爭取）；可退/有條件退/可退性未被否定 → C07。不可抗力事由、瑕疵/供應商無法履約/不可改重訂的優先序不變，純個人事由（沒時間去/行程有變/買錯）明列為非不可抗力
- 落點（本版快照）：總綱速查＋根因匹配鐵律各補可退性前提；C06 首次帶 `calibration`、`likely_cause_guide` 重寫（判死即選「規則就是不可退用戶不滿」，不要求用戶表態）；C07 新增 `calibration`，guide 補「禁由開口問退款反推事前不知規則，無明示證據→unclear」；判例庫「其他跨類 verbatim」補本案與反面對照各一條
- 連帶修正：C06 上線後 `sentiment` 出現漂移（實測 2/3 誤判 negative，疑受 cause 字面「用戶不滿」帶偏）→ neutral 條列補「規則判死但用戶未再爭取仍為 neutral；likely_cause 的『用戶不滿』字面不構成 negative 依據」
- SSOT 同步：`config/ai_judge/after_sales_root_cause.json` C06/C07 補 `calibration`（繼 C12/C13 後第二組）；域判官 `prompts/debug/domains/theme_101/v2.md` 速記分界同步改為可退性軸
- 驗證（gpt-5.4-mini，調試台實跑）：本案**新版 5/5 正解**（category＋cause＋sentiment=neutral），舊版同題 3 次跑出 2 次 C07 誤判、1 次 OOT（本身不穩）。金標子集回歸（`exam_first100` 中 [101] 全類 ＋ OOT 含取消/退款關鍵詞者，共 36 筆）：**新版兩次跑 33/36、31/36，舊版 30/36**（差幅在 LLM 抖動範圍內，不宣稱淨提升）；關鍵是**兩次跑的 miss 都是舊版既有 miss 的子集、未引入新 miss**，且兩次「取消政策本身僵化」皆**0 筆誤收**（無過度矯正）、gold=C07 的 4 筆 category 與 cause 兩次全守住
- 表側待辦（PM）：《根因標籤架構》C07 的 Examples「沒注意到不可退條款」與 Definition「可退或有條件退」互斥，需擇一（建議該例移至 C06），否則下次 taxonomy 重生會掉回舊口徑

## 目錄改制 — 2026-07-27（vN.md ＋ 雙契約切換 → 時間戳一版一檔）

舊做法是 `v2.md`/`v3.md` 並存、前端以「輸出契約」radio 切換，實測的問題是**同時存在兩份口徑**：
頁面上調的是一份、離線跑批跑的是另一份，兩邊會各自漂移（v3 快照與 tmp 作業線的渲染版一度在 OOT 哨兵、
theme 是否帶空格、sentiment/flag 定義上分岔 320 行）。改制後：

- `v3.md` → `2026-07-27-185628.md`（原內容不動，時間戳取該版 commit 時間）；`v2.md` 與模板 `after_sales_root_cause.md` 刪除
- v2 契約（urgency_flag 布林＋tail_theme＋null 不適用欄）全棧清退：schema／校驗器／欄位卡／`contract` 參數／前端 radio 一併移除
- 跑批 manifest 的 `contract` 欄改記 `prompt_version`（空＝送出前在頁面上臨時編輯過，實際內容以 run 目錄的 `system_prompt.md` 與 `prompt_sha256` 為準）

## v3 — 2026-07-27（現行唯一契約；原名 v3.md，改制後為 `2026-07-27-185628.md`）

v1/v2 是同一輸出契約的格式改版；**v3 換了輸出契約本身**（《AI 判定其他欄位定義》定案版 2026-07-22）：

- 欄位：`keywords` 陣列（1–5 個×2–6 字，全 session 必填，事由→訴求→對象，僅取 [USER]）、`urgency` 由布林升 1–5 整數（≥4 觸發高優先、轉真人不自動高分）、新增 `no_actionable_content`（true ⇒ OOT∧殘段∧keywords=[]）、裁撤 `tail_theme`、全欄禁 null（不適用填 `n/a`）
- 判準：內嵌 260 筆金標調參的實測校準層（calibration／likely_cause_guide／跨類判例庫／六條 cause 硬規則，血緣＝tmp 作業線 v_hw14→v3）；「單據問題錯置到供應商線」補決勝規則（供應商線單據訴求一律歸錯置、平台線依訴求歸前四類——維度重疊實測會穩定吃掉訴求訊號，見 2026-07-27 合成單據 demo）
- 形態：**靜態快照**（分類庫已內嵌，非模板渲染）——校準層僅 [93] C12/C13 兩條併入 `config/ai_judge/after_sales_root_cause.json` SSOT（見 2026-07-27 加購方向增補），其餘仍只在本快照；受控值（類名/causes/theme 全稱字面）生成時已對齊 config，config 若改受控值需重生本檔。**待辦**：把校準層整批併入 SSOT 改走模板渲染（目前僅 [93] C12/C13 兩條在 SSOT）
- ~~消費端：調試台雙契約並存（`body.contract` 擇一），v2 仍為正式批次口徑~~ → **已由上方「目錄改制」取代**：本契約為唯一契約，schema/校驗器不再依 `contract` 切換

### v3 校準層增補 — 2026-07-27（同檔就地修，未升版號）

**[93]↔[COMM]↔[101] 三向邊界：連線商品「加購延長使用天數」**。真實跑批誤判案（WiFi 機用戶「怎麼加購延長一天？訂購都要三天起跳」→ 因颱風要求延長至 7/14）被判 `[COMM] 連線商品使用方式或設定諮詢`；Gemini 另有票數漂向 `不可抗力特例申請無通道`（該類校準原就寫「因不可抗力想延期/延後（如延後還機）」，字面正中本案）。正解＝`[93] 特殊需求/加購無自助入口`＋`系統缺特殊需求欄位`＋`改人數/加購`。

- 依據：線上《根因標籤架構》表口徑——加購屬訂單層操作，[COMM] 軸線是「能不能用」（安裝/連線/使用規則），不含訂單層加購；颱風僅為事由，用戶未爭取退改減免即不觸發不可抗力
- 三處校準：[93] 加購類新增 `calibration`（連線商品延長天數歸本類＋雙向排除理由）＋cause/target guide 各補一句（「三天起跳」門檻→系統缺特殊需求欄位；延長天數→改人數/加購）；[COMM] 使用諮詢 `calibration` 補轉向（延長辦理→[93]，純問使用期限才留本類）；[101] 不可抗力 `calibration` 補「延後還機/延長使用」細分（爭取特例通融→本類、願付費加購→[93]）
- 判例庫兩條 verbatim：颱風路由簇、[COMM] 簇各一條，分別堵兩個漂移方向
- 驗證：調試台 v3 契約走 app 連線實跑本案 → 四欄全對、`valid=true` 零校驗問題（gpt-5.4-mini）；tmp 作業線同步同一修正，gpt-5.4-mini 與 Gemini 各 5 票全對，三條回歸（eSIM 提前裝時程、網路怎麼用單問、金標 #18 特例延後還機）各 5/5 不動
- 待辦：線上表側建議 PM 於 [COMM] 使用諮詢的 Exclude 欄補「要求延長使用天數/加購天數 → [93] 特殊需求/加購無自助入口」（表為真相源，補上才不會在下次 taxonomy 重生時掉回去）

### 加購天數「方向不拘」增補 — 2026-07-27（v2 SSOT ＋ v3 快照同步就地修，未升版號）

**誤判案**（v3.2 跑批 session 690677，WiFi 機）：颱風假設性詢問開場 → 航班提前，用戶要「改到明天 6:00 領機、還機時間不變、我可以多付錢」，客服請其另下 1 天短單再人工合併兩單。判成 `[93] 修改受限`＋`商品規則不允許改`＋`改日期/時段/班次`；正解＝`特殊需求/加購無自助入口`＋`系統缺特殊需求欄位`＋`改人數/加購`（同金標 #83 組合）。

- 兩個洞：①既有加購校準只寫「延後歸還」語感，模型沒把「提前領取」對映成加購天數；②「修改受限」缺前提，需求最終被客服協助達成也被讀成「不允許改」
- v3 快照（判例庫）：颱風簇補「航班提前要提早領取」verbatim 一條 ＋ 緊接反面對照一條（分界＝有無加購/付費/下單動作，堵回金標 #18「延後還機求通融」）；「被規則擋→取消重訂」標的分流補前提「真的被擋、改不成」；WiFi 加購判例補「提早一天領機」與「日期只能選 X-Y」線索
- v3 快照（分類庫）：C13 加購類 `calibration` 補方向不拘＋另下短單合併＝無自助入口證據；`likely_cause_guide` 補「選不到目標日期區間」；C12 修改受限新增 `calibration`（前提＝最終確定改不了）
- **v2 SSOT**：`config/ai_judge/after_sales_root_cause.json` 的 C12/C13 首次帶 `calibration` 欄（模板 `prompts/debug/after_sales_root_cause.md` 同步宣告「calibration 優先於 Definition/Include/Exclude 字面」）；C12 另含「被擋→取消重訂」標的分流與「取消已實際辦成→[101]」判準，C13 含「只求通融無付費/下單＝[101]」反面
- 驗證（gpt-5.4-mini，調試台實跑，每案 3 票取多數）：**v2** 本案 3/3 正解（改前 3/3 誤判）；回歸 #18 3/3、#59 3/3（改前僅 1/3 完全命中）、#61 2/3、#83 3/3、#92 category 3/3（cause 與改前同為 `商品不支援原單修改`，非本次引入）。**v3** 本案 3/3、#18 3/3、#59 3/3、#61 3/3、#83 3/3
- 同步：tmp 作業線 `build_taxonomy_v3.py` / `prompt_v3.md` 同一修正並重生 `taxonomy_v3.json`＋`prompt_final_rendered_v3.md`；域判官 `theme_93/v2.md`（加購方向＋反面棄權）、`theme_101/v2.md`（願付費加購棄權）同步邊界

## v2 — 2026-07-22（現行）

依 [Claude Fable 5 prompting 指南](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5) 重寫格式，**判準語義不變**：

- 開頭補任務意圖（判定進根因統計、精準優於覆蓋、unclear/OOT 的存在理由）——give the reason, not only the request
- 受控資料改用 `<taxonomy>` / `<oot_subtype_options>` / `<modify_target_options>` XML 標籤定界，資料與指令明確分離
- 原 13 條編號混排規則按職責重組為「裁決原則／欄位判定規則／資料處理守則／輸出契約／裁決流程」，重複禁令收斂為一處
- 「逐字取值」「只輸出 JSON」等輸出約束集中至輸出契約節
- 補顯式 JSON 輸出契約範例（原版僅文字描述欄位；強化 response_format 降級為 json_object / 純 Prompt 約束時的穩健性）

## v1 — 2026-07-22

- 調試台上線版快照（原 `prompts/debug/after_sales_root_cause.md` 平鋪檔）
