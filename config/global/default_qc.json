{
  "_comment": "QC DB（PostgreSQL）連線預設——跨語言共用非機密單一真相源（前端 @config/global/default_qc.json、後端 settings.py 同讀）。機密（密碼）走 backend/.env / user_settings，勿放這裡。port/schema 為跨環境共用；環境（SIT/Stage）各帶 host + bootstrap database（測試連線/列舉 database 用的起手庫）。實際抽取的 database 由前端多選，存 user_settings。",
  "port": 5432,
  "schema": "public",
  "defaultEnv": "sit",
  "environments": [
    {
      "id": "sit",
      "label": "SIT",
      "host": "postgresql-qc.sit.kkday.com",
      "name": "qc201_kkdb_product"
    },
    {
      "id": "stage",
      "label": "Stage",
      "host": "postgresql-qc.stage.kkday.com",
      "name": "kkdb_product"
    }
  ]
}
