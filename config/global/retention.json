{
  "_comment": "資料保留期政策（維運可調，改此檔即生效，無需改碼）。由 scripts/ops/retention.py 消費，預設 dry-run 只報不刪。各項的判準見下方 _rationale——訂閾值前先想清楚『這份資料在什麼時間點之後不再有人讀』，而非憑感覺設天數。",

  "llm_usage_lst": {
    "_rationale": "per-call 用量明細只被聚合消費（成本 dashboard 按日/月彙總），沒有任何 per-call 的 UI。半年前的單筆呼叫紀錄無人查詢，但保留期要夠長以覆蓋『上季成本回溯』這種需求。",
    "delete_older_than_days": 180
  },

  "prejudge_run_log_lst": {
    "_rationale": "初判執行日誌（逐筆 LLM 對話快照，一則評論一列，實測約 43 KB/則）。實際回看窗只有『跑完當下看一眼』與『這則判錯了回頭查為什麼』，一個月後無人問津；run 的統計列（筆數/成功失敗/token/費用）在 prejudge_run_tbl 另存並長期保留，清日誌不影響成本回溯。全量批一次就是數萬列，這是本庫成長最快的表——保留期不可省。",
    "delete_older_than_days": 30
  },

  "attribution_event_lst": {
    "_rationale": "failure 事件的查詢條件是『該 source_id 最近一次成功初判之後的失敗』，一旦成功初判過，舊的 failure 永遠讀不到。prejudge 快照則是多模型對比與匯出的資料源，但兩者都只取 DISTINCT ON 最新一筆——保留數筆供人工回溯演進即足夠。note 為人工輸入，永久保留。",
    "delete_superseded_failures": true,
    "keep_prejudge_snapshots_per_model": 3
  },

  "prompt_debug_batch_artifacts": {
    "_rationale": "跑批產物（輸入原檔 + jsonl + csv）落在 data/prompt_debug_batch/<run_id>/，目前零清理、永久堆積。三個月後的跑批產物已無人下載。",
    "delete_dirs_older_than_days": 90
  }
}
